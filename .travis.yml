dist: travis_latest
group: edge
sudo: false
language: c
os: linux
script:
  - mkdir build && (cd build && cmake .. -DBUILD_SHARED_LIBS:BOOL=ON -DCMAKE_VERBOSE_MAKEFILE:BOOL=ON  && cmake --build . && ctest -V)
